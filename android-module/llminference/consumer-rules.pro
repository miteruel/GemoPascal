# No consumer-side rules needed: this .aar is consumed by RAD Studio, not by another
# Gradle/R8 build, so consumer ProGuard rules are never actually applied by a downstream
# Gradle project. Kept for completeness / in case this module is ever reused from Gradle.
